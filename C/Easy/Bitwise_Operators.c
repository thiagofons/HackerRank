#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
//Complete the following function.


void calculate_the_maximum(int n, int k) {
    int and, or, xor;
    
    for(int i = n; i <= k; i++){
        for(int j = i + 1; j <= k; j++){
            if(i != j){

            }
        }
    }
}

int main() {
    int n, k;
  
    scanf("%d %d", &n, &k);
    calculate_the_maximum(n, k);
 
    return 0;
}
